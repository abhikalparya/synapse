# Copy to `.env` in the repo root or in `backend/` (both paths are loaded).
OPENAI_API_KEY=

# Optional OpenAI client settings
# OPENAI_ORGANIZATION=
# OPENAI_PROJECT=

# Optional Synapse tuning
# WIKI_WRITEBACK_CONFIDENCE_THRESHOLD=0.6
# GENERATE_CONCURRENCY=4
# CONSOLIDATION_STRING_THRESHOLD=0.82
